service gsiftp
{
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = root
    instances   = UNLIMITED
    cps         = 400 10
    server      = /home/meloam/ANSE/local/sbin/gridftp-lfs-inetd
    disable     = yes
}
